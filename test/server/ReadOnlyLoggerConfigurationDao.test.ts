import * as assert from "assert";
import ReadOnlyLoggerConfigurationDao from "/imports/server/dao/ReadOnlyLoggerConfigurationDao";

describe("ReadOnlyLoggerConfigurationDao", function(){
    it("")
    it("needs to be tested", function(){
        // this.timeout(500000000);
        // const findStub = sinon.stub(Mongo.Collection.prototype, "find");
        // const observeChangesStub = sinon.stub(Mongo.Cursor.prototype, "observeChanges");
        // observeChangesStub.callsFake((obj) => {
        //     obj.added("a", {_id: "a"});
        //     obj.changed("a", {_id: "a"});
        //     obj.remove("a");
        // });
        // observeChangesStub.returns(observeChangesStub);
        // const x = new ReadOnlyLoggerConfigurationDao(null);
        //
        // sinon.restore();
        assert.fail("do me");
    });
});
